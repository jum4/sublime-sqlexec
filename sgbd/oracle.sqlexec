{
    "sql_exec": {
        "queries": {
            "desc": "select table_name as tables from user_tables;",
            "desc table": "select * from user_tab_columns where table_name = '%s' order by column_id;",
            "show records": "select * from %s;"
        }
    }
}
